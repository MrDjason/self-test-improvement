# ==================== 导入模块 ====================
import torch
# ==================== 7.2.1 利用MSE进行计算 ====================
# MSE loss函数存在很多局部极小值，寻找其他极小值
# ==================== 7.2.2 交叉熵损失函数 ====================
# 使用二分类交叉熵损失函数BCELoss
'''
y真实标签,只有0、1,ŷ预测概率
y=1
BCELoss = -ln(ŷ)
y=0
BCELoss = -ln(1-ŷ)
正例要奖励预测概率高的情况，负例要奖励预测概率低的情况
两者逻辑相反，必须分开计算才能合理量化损失。

ŷ=1,损失为-ln1=0 毫无偏差
ŷ=0,损失为-ln0=∞ 完全偏离正确结果
'''

# 综合后 → BCELoss(y,ŷ) = -[yln(ŷ)+(1-y)ln(1-ŷ)]

